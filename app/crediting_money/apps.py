from django.apps import AppConfig


class CreditingMoneyConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'crediting_money'
    verbose_name = 'Заявки на пополнение и реквизиты'
